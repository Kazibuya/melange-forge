package:
  name: healthcheck-fcgi
  version: "1.0.0"
  epoch: 0
  description: healthcheck via FastCGI TCP socket for distroless image
  copyright:
    - license: MIT

environment:
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
    packages:
      - go-1.26

pipeline:
  - uses: go/build
    with:
      output: healthcheck-fcgi
      packages: .
      go-package: go-1.26
